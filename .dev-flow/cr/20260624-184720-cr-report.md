# CR Report

## Scope
- Review target: 当前未提交 Git diff，以及全量 `skills/`、`.opencode/skills/`、命令/安装/发布相关验证面。
- Evidence reviewed: `git status --short --branch`, `git diff --stat`, targeted `git diff` for modified skills and references, `node bin/dev-flow.mjs doctor --target .`, `git diff --check`, `node --check bin/dev-flow.mjs`, `npm pack --dry-run`, `npm view dev-flow-skills version`, GitHub Publish run `28089245264`, skill-creator `SKILL.md`, and attempted `quick_validate.py` output.
- Review limits: 未修复实现文件；未执行真实安装到用户全局目录；`skill-creator/scripts/quick_validate.py` 因当前 Python 环境缺少 `yaml` 模块未能完成。

## Score
- Score: 64
- Status: cr_blocked

## Findings
| Severity | 问题 | 影响 | 证据 | 建议 | 状态 |
|---|---|---|---|---|---|
| P1 | `skills/` 的核心 skill 修改没有同步到 `.opencode/skills/`。 | OpenCode 安装面与 Codex/Claude 安装面行为分裂；`doctor` 失败，发布包会同时携带两套不一致的核心治理规则，可能导致用户在不同平台走不同流程。 | `node bin/dev-flow.mjs doctor --target .` 报告 29 个 `core skill mirror` 失败；`diff -qr skills/<core> .opencode/skills/<core>` 显示除少数模板外几乎所有核心 skill/reference 均不同步。 | 将所有 core dev-flow skill 的源变更同步到 `.opencode/skills/`，或明确调整 mirror 策略和 doctor 规则；同步后重跑 doctor。 | blocking |
| P1 | `dev-flow-cr` 改写后缺少 doctor 要求的 CR 触发短语。 | 仓库内置语义检查失败，CI/发布前检查不能通过；虽然语义接近，但自动质量门会判定 source governance 不合格。 | [bin/dev-flow.mjs](/Volumes/paul.lee/Code/dev-flow-skills/bin/dev-flow.mjs:96) 要求 `Run only when the user explicitly requests CR`；[skills/dev-flow-cr/SKILL.md](/Volumes/paul.lee/Code/dev-flow-skills/skills/dev-flow-cr/SKILL.md:12) 改成 `This skill is invoked only after...`；doctor 输出 `missing: Run only when the user explicitly requests CR`。 | 恢复或包含 doctor 要求的原短语，或同步更新 doctor required phrase，但必须保证 `.opencode` 镜像一致。 | blocking |
| P2 | 两个超过 100 行的 reference 缺少 Table of Contents。 | 违反 skill-creator/doctor 的 progressive disclosure 规范；长 reference 首屏不可快速了解结构，增加后续 agent 误读和漏读风险。 | [skills/dev-flow-acceptance/references/readiness-and-report.md](/Volumes/paul.lee/Code/dev-flow-skills/skills/dev-flow-acceptance/references/readiness-and-report.md:1) 101 行无 TOC；[skills/dev-flow-master/references/routing-and-complexity.md](/Volumes/paul.lee/Code/dev-flow-skills/skills/dev-flow-master/references/routing-and-complexity.md:1) 105 行无 TOC；doctor 报告 `reference TOC` 失败。 | 添加 `## Table of Contents`，或压缩到 100 行以内；同步 `.opencode` 后重跑 doctor。 | fix_required |
| P2 | `dev-flow-cr` 的边界表述从“用户显式 CR 即可运行”扩大/混合为“acceptance_ready 后或用户显式运行”。 | 与独立 `/dev-flow-cr` 命令的定位略有歧义：可能让 agent 误以为 acceptance_ready 后可以自动触发 CR，削弱“用户触发”的边界。 | [skills/dev-flow-cr/SKILL.md](/Volumes/paul.lee/Code/dev-flow-skills/skills/dev-flow-cr/SKILL.md:12) 写 `after acceptance_ready ... OR when the user explicitly runs /dev-flow-cr`；而命令设计和前序规则强调 CR 不自动运行。 | 建议改成“仅当用户显式请求 CR 或运行 `/dev-flow-cr`；通常在 acceptance_ready 之后使用”，避免把 acceptance_ready 写成触发条件。 | fix_required |
| P3 | `quick_validate.py` 未能运行，缺少本地 `yaml` 依赖。 | 不是 skill 内容本身失败，但 skill-creator 推荐校验无法完成，降低基础 frontmatter 校验置信度。 | `python3 .../quick_validate.py skills/<skill>` 全部报 `ModuleNotFoundError: No module named 'yaml'`。 | 使用带 PyYAML 的 Python 环境重跑，或补充项目内可复现的校验命令。 | acknowledged |

## Verification Notes
- Commands inspected or recommended:
  - `node bin/dev-flow.mjs doctor --target .`: failed
  - `git diff --check`: passed
  - `node --check bin/dev-flow.mjs`: passed
  - `npm pack --dry-run`: passed, but package would include divergent `skills/` and `.opencode/skills/`
  - `npm view dev-flow-skills version`: `0.1.14`
  - `gh run view 28089245264`: GitHub Publish workflow previously succeeded for commit `cf636fd`
  - `diff -qr skills/<core> .opencode/skills/<core>`: failed mirror consistency across core skills
  - `skill-creator/scripts/quick_validate.py`: not completed because PyYAML is unavailable
- Tests missing or not run:
  - No successful full doctor after the current diff.
  - No successful skill-creator quick validation due local dependency gap.
  - No fresh Codex/Claude/OpenCode temporary install doctor after current dirty diff.

## Decision
- Blocking findings: 2 P1 findings; do not commit, publish, or merge current diff as-is.
- P2 defer decision needed: none recommended; P2 items should be fixed before re-review because the same doctor gate already fails.
- Suggested next owner: implementation pass through `dev-flow-master`/`skill-creator` to sync `.opencode`, restore/update CR semantic checks, add TOCs, then rerun full doctor and `/dev-flow-cr`.
