interface:
  display_name: "Dev Flow Loop"
  short_description: "Goal-preserving Loop Engineering controller"
  default_prompt: "Use $dev-flow-loop to confirm loop-only baseline artifacts, create a phase DAG, and drive dev-flow rounds inside an approved envelope."
